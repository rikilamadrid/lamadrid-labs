// PROTOTYPE — throwaway. Renders the standalone Work section (feature 40)
// in isolation for manual verification ahead of homepage assembly (42).
import { Work } from "@/components/sections/Work";

export default function WorkDevPage() {
  return (
    <main className="bg-lab-bg text-lab-ink">
      <Work />
    </main>
  );
}
