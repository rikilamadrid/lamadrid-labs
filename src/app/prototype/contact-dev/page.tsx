// PROTOTYPE — throwaway. Renders the standalone Contact section (feature 41)
// in isolation for manual verification ahead of homepage assembly (42).
import { Contact } from "@/components/sections/Contact";

export default function ContactDevPage() {
  return (
    <main className="bg-lab-bg text-lab-ink">
      <Contact />
    </main>
  );
}
